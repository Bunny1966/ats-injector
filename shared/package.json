{
  "name": "@resume-optimizer/shared",
  "version": "1.0.0",
  "description": "Shared TypeScript types for AI Resume Optimizer",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "rimraf": "^6.0.0"
  }
}
